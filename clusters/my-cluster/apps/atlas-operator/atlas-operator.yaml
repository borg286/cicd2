apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: atlas-operator
  namespace: flux-system
spec:
  interval: 1h
  chart:
    spec:
      chart: atlas-operator
      version: "0.1.0"
      sourceRef:
        kind: HelmRepository
        name: ariga
        namespace: flux-system
  install:
    createNamespace: true
